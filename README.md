# NinkyClicker

An efficient, lightweight yet configurable autoclicker for Windows supporting both global and singular processes. 

## Usage

`NinkyClicker.exe <cps> <program> <type[virtual|hook]>` or just run the .exe directly

